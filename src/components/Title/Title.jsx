// Composant Title
function Title(props) {
    return (
        <div className="title-container">
            <h1 className="game-title">{props.text}</h1>
        </div>
    );
}

export default Title;
